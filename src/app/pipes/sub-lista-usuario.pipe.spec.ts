import { SubListaUsuarioPipe } from './sub-lista-usuario.pipe';

describe('SubListaUsuarioPipe', () => {
  it('create an instance', () => {
    const pipe = new SubListaUsuarioPipe();
    expect(pipe).toBeTruthy();
  });
});
